import { useState } from "react";
import { useHistory } from 'react-router-dom';

const Create = () => {
    return ( 
        <div className=" create py-10" style={{right:"45%"}}>
            <h2>Add new Note</h2>
        </div>
     );
}
 
export default Create;